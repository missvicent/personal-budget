export PATH="$HOME/.npm-global/bin:/usr/bin:/bin:/usr/local/bin:$PATH"

if [ package.json -nt package-lock.json ]; then
  echo "ERROR: package-lock.json may be out of sync with package.json"
  echo "Run 'npm install' to update the lockfile, then commit again."
  exit 1
fi

npx lint-staged
